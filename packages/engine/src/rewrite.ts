/**
 * Deterministic, force-preserving rewrite of a message's head act.
 *
 * NOT YET SPECIFIED. SPEC.md, LEXICON.md, and EVAL.md currently define no
 * rules, algorithm, or fixtures for this feature (see CLAUDE.md "Known
 * gaps" #1). Do not implement until:
 *   1. rewrite rules are specified in SPEC.md (or a new REWRITE.md) with
 *      the same normative status as the rest of the engine, and
 *   2. fixtures covering it exist in EVAL.md / eval/,
 * per the test-first rule (CLAUDE.md rule 8).
 */
